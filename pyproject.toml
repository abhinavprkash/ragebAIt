[project]
name = "ragebait-poster"
version = "0.1.0"
description = "Social Media Poster Agent for ragebAIt"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "browser-use>=0.1.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]
